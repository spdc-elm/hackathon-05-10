示例 TXT 教材

第一章 绪论

这是第一章正文。TXT 没有显式 Markdown 标题，所以 parser 需要靠规则识别章标题。

第一节 背景

低摩擦解析比复杂抽象更重要。

1. 保留普通编号条目。
2. 不要把所有短行都误判成标题。

第二章 方法

这一章用于确认 TXT parser 能按章切分。

一、规则解析

规则解析应该是首版 fallback，LLM 辅助只能作为增强路线。
